const input = require('prompt-sync')()

function main(){
    let valor_compra = Number(input('Digite aqui o valor da compra: '));
    let recebimento = Number(input('Digite aqui o valor do recebimento: '));

    while (recebimento_inicial, simular_vendas){
        if (recebimento === 2 && parcelas === 0){
            resultado = recebimento_processamento(juros_02dias_a_vista, pagamento_parcelas)
        }else if (recebimento === 2 && parcelas > 0){
            resultado = recebimento_processamento(juros_02dias_a_prazo, pagamento_parcelas)
        }else{
            resultado = (valor_compra % 2 === 0 && recebimento_inicial <= 1)
        }
    }

function lojista(){
    const nome_lojista = Number(input('Nome do atendente: '))
}

    console.log(nome_lojista)

}

lojista()
